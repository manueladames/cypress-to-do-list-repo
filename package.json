{
  "name": "cypress-repo",
  "version": "1.0.0",
  "description": "E2E automation for todolist.james.am with Cypress and POM",
  "scripts": {
    "cy:open": "cypress open",
    "cy:run": "cypress run",
    "cy:run:smoke": "cypress run --env grepTags=@smoke",
    "cy:run:regression": "cypress run --env grepTags=@regression"
  },
  "devDependencies": {
    "@cypress/grep": "^3.1.4",
    "cypress": "^13.6.0"
  }
}
